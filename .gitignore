node_modules/
dist/
photos/
.env
.env.local
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.log

# wrangler
.wrangler/
.dev.vars
.dev.vars.*

# generate-albums.cjs upload/sync state
.r2-upload-manifest.json
# Glob covers both the --remote (.d1-sync-manifest.json) and --local
# (.d1-sync-manifest.local.json) variants -- the latter had leaked a private
# album's secretCode into git before this was widened.
.d1-sync-manifest*.json